// Release Configuration
// This file contains release-specific build settings

// Include secrets (git-ignored)
#include "Secrets.xcconfig"

// API Base URLs (can be overridden in Secrets.xcconfig)
API_BASE_URL = https:/$()/api.piratenpartei.de
DISCOURSE_BASE_URL = https:/$()/forum.piratenpartei.de
MEINE_PIRATEN_BASE_URL = https:/$()/meine.piratenpartei.de

// App Configuration
PRODUCT_BUNDLE_IDENTIFIER = de.piratenpartei.app
