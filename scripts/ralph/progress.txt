# Ralph Progress Log
Started: Sa. 31 Jan. 2026 17:35:42 CET
---

## Codebase Patterns

### Discourse API Client Architecture
- Use `DiscourseAPIClient` for all Discourse API calls (not the repository directly)
- API client returns raw `Data` for decoding flexibility; repository maps to domain models
- Error handling: `DiscourseError` provides German localized messages for UI display
- Auth: Bearer token injected via `AuthenticatedHTTPClient`; no explicit token handling in API client

---

## [2026-02-01 00:30] - M3B-004
- What was implemented:
  - Created `MessageThread.swift` domain model for private message threads
  - Added DTOs for private messages response: `DiscoursePrivateMessagesResponse`, `DiscoursePrivateMessageTopicListDTO`, `DiscoursePrivateMessageTopicDTO`
  - Extended `DiscourseRepository` protocol with `fetchMessageThreads(for:)` method
  - Implemented `fetchMessageThreads` in `RealDiscourseRepository` using existing `DiscourseAPIClient.fetchPrivateMessages()`
  - Implemented `fetchMessageThreads` in `FakeDiscourseRepository` with stub PM data
  - Created `MessagesViewModel.swift` with full state handling matching ForumViewModel pattern
  - Replaced placeholder `MessagesView` with full implementation showing PM threads list
  - MessagesView displays: thread title (with unread indicator), participants, post count, last activity time
  - Loading/empty/error states are implemented with proper German localization
  - Wired MessagesViewModel through AppContainer, MainTabView, RootView, and PIRATENApp
- Files changed:
  - PIRATEN/Core/Domain/Discourse/MessageThread.swift (new)
  - PIRATEN/Core/Data/Discourse/DiscourseDTO.swift (added PM DTOs)
  - PIRATEN/Core/Domain/Discourse/DiscourseRepository.swift (added fetchMessageThreads)
  - PIRATEN/Core/Data/Discourse/RealDiscourseRepository.swift (implemented fetchMessageThreads)
  - PIRATEN/Core/Data/Discourse/FakeDiscourseRepository.swift (implemented fetchMessageThreads + stub data)
  - PIRATEN/App/ViewModels/MessagesViewModel.swift (new)
  - PIRATEN/App/Views/Main/MessagesView.swift (full implementation replacing placeholder)
  - PIRATEN/App/Views/Main/MainTabView.swift (added messagesViewModel prop, localized tab to "Nachrichten")
  - PIRATEN/Core/Support/AppContainer.swift (added messagesViewModel)
  - PIRATEN/App/RootView.swift (added messagesViewModel passthrough)
  - PIRATEN/App/PIRATENApp.swift (wired messagesViewModel from container)
  - scripts/ralph/prd.json (marked M3B-004 passes: true)
- **Context7 / Web Research Summary:**
  - Discourse PM endpoint: GET `/topics/private-messages/{username}.json`
  - Response structure mirrors `/latest.json` with `users` array and `topic_list.topics`
  - PM topics have same fields as regular topics: `id`, `title`, `posts_count`, `created_at`, `last_posted_at`
  - `posters` array contains participant references with `user_id` and `description` (e.g., "Most Recent Poster")
  - `unseen` field indicates whether thread has unread posts
  - PMs are stored as topics with `archetype='private_message'` - fetching single PM uses same `/t/{id}.json` endpoint
  - Sources: https://meta.discourse.org/t/api-how-to-get-all-private-messages-that-include-a-user-including-sent-messages/214655, https://docs.discourse.org/
- **Learnings for future iterations:**
  - MessagesViewModel needs both discourseRepository and authRepository (to get current user's username)
  - Pattern established: views using repository data get viewModel from AppContainer through view hierarchy
  - MessageThread model is separate from Topic since PM has different semantics (participants vs category)
  - Reusing existing DiscourseAPIClient methods reduces code duplication
---

## [2026-01-31 23:45] - M3B-003
- What was implemented:
  - Created `TopicDetailView.swift` with full state handling (idle/loading/loaded/notAuthenticated/authenticationFailed/error)
  - Created `TopicDetailViewModel.swift` to coordinate between view and repository
  - Added DTOs for topic detail response: `DiscourseTopicDetailResponse`, `DiscoursePostStreamDTO`, `DiscoursePostDTO`
  - Implemented `fetchPosts(forTopicId:)` and `fetchTopic(byId:)` in `RealDiscourseRepository`
  - Added navigation from `ForumView` to `TopicDetailView` via `NavigationLink`
  - Added `makeTopicDetailViewModel(for:)` factory method to `AppContainer`
  - Posts display with author, content excerpt (HTML stripped), post number, timestamp, reply/like counts
  - Error states provide retry button; auth errors show login prompt
- Files changed:
  - PIRATEN/App/Views/Main/TopicDetailView.swift (new)
  - PIRATEN/App/ViewModels/TopicDetailViewModel.swift (new)
  - PIRATEN/Core/Data/Discourse/DiscourseDTO.swift (added topic detail DTOs)
  - PIRATEN/Core/Data/Discourse/RealDiscourseRepository.swift (implemented fetchPosts/fetchTopic)
  - PIRATEN/App/Views/Main/ForumView.swift (added navigation + factory prop)
  - PIRATEN/App/Views/Main/MainTabView.swift (added factory prop passthrough)
  - PIRATEN/App/RootView.swift (added factory prop passthrough)
  - PIRATEN/App/PIRATENApp.swift (wired factory from AppContainer)
  - PIRATEN/Core/Support/AppContainer.swift (added makeTopicDetailViewModel factory)
  - scripts/ralph/prd.json (marked M3B-003 passes: true)
- **Context7 / Web Research Summary:**
  - Discourse `/t/{topic_id}.json` returns topic metadata + `post_stream` object
  - `post_stream.posts[]` contains first 20 posts; `post_stream.stream[]` contains all post IDs
  - Post fields: `id`, `post_number`, `username`, `name`, `avatar_template`, `created_at`, `cooked` (HTML content), `reply_count`, `reads`, `like_count`
  - Dates are ISO 8601 format with optional fractional seconds
  - Sources: https://meta.discourse.org/t/api-getting-all-posts-in-a-topic/41018, https://meta.discourse.org/t/fetch-all-posts-from-a-topic-using-the-api/260886
- **Learnings for future iterations:**
  - ViewModel factory pattern works well for on-demand view model creation with DI
  - SwiftUI NavigationLink with factory closure enables clean navigation while preserving DI
  - HTML stripping via regex is simple for excerpts; full HTML rendering would need AttributedString or WebView
  - `compactMap` over posts handles graceful failure when date parsing fails
---

## [2026-01-31 21:11] - M3B-002
- What was implemented:
  - Verified RealDiscourseRepository already implements DiscourseRepository with `/latest.json` fetching
  - DiscourseDTO.swift provides mapping from API response to Domain Topic model:
    - `DiscourseLatestResponse` → parses root with `users` and `topic_list`
    - `DiscourseTopicDTO` → includes `toDomainModel(users:)` for clean domain conversion
    - `DiscourseUserDTO` → maps to `UserSummary` with avatar URL resolution
  - ForumView already has complete state handling:
    - `.idle/.loading` → ProgressView
    - `.loaded` with empty → empty state with refresh button
    - `.loaded` with topics → topic list with pull-to-refresh
    - `.notAuthenticated` → login prompt with "Anmeldung erforderlich" message
    - `.authenticationFailed` → re-login prompt with "Sitzung abgelaufen" message
    - `.error` → error state with retry button
  - AppContainer wires RealDiscourseRepository with DiscourseAPIClient and AuthenticatedHTTPClient
- Files changed:
  - scripts/ralph/prd.json (marked M3B-002 passes: true)
- **Context7 / Web Research Summary:**
  - Discourse /latest.json returns nested JSON with `topic_list.topics` array and `users` array
  - Topics contain `posters` array with `user_id` references for looking up authors
  - Date format is ISO 8601 with optional fractional seconds
  - Avatar URLs use `{size}` placeholder template pattern (e.g., 120 for medium)
  - Sources: https://docs.discourse.org/, https://github.com/discourse/discourse_api_docs
- **Learnings for future iterations:**
  - M3B-002 was already fully implemented during M3B-001 scaffolding
  - The DTO-to-Domain mapping pattern with `toDomainModel()` is clean and testable
  - `compactMap` over topics handles graceful failure when poster lookup fails
---

## [2026-01-31 22:15] - M3B-001
- What was implemented:
  - Created `DiscourseAPIClient.swift` with authenticated HTTP client integration
  - Created `DiscourseError.swift` for domain-level Discourse errors
  - API client uses `AuthenticatedHTTPClient` which injects `Authorization: Bearer <token>` header
  - Implemented request helpers for: `/latest.json`, `/t/{id}.json`, `/topics/private-messages/{username}.json`
  - Error mapping from HTTP status codes to user-friendly German error messages
- Files changed:
  - PIRATEN/Core/Data/Discourse/DiscourseAPIClient.swift (new)
  - PIRATEN/Core/Data/Discourse/DiscourseError.swift (new)
  - scripts/ralph/prd.json (marked M3B-001 passes: true)
- **Context7 / Web Research Summary:**
  - Discourse API supports authentication via HTTP headers (not just URL params)
  - Standard API keys use `Api-Key` + `Api-Username` headers
  - User API keys use `User-Api-Key` header
  - OAuth2 bearer tokens use `Authorization: Bearer <token>` header
  - Rate limits for authenticated users: 20 requests/minute, 2,880 requests/day
  - Source: https://meta.discourse.org/t/api-can-i-authenticate-without-putting-the-key-in-the-url/29425
  - Source: https://meta.discourse.org/t/user-api-keys-specification/48536
- **Learnings for future iterations:**
  - The project uses `PBXFileSystemSynchronizedRootGroup` - new files are auto-added to build
  - Discourse PMs are topics with `archetype='private_message'` - same endpoint works
  - Discourse error responses have shape: `{ "errors": [...], "error_type": "..." }`
---
