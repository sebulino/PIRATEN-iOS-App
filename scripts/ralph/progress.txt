# Ralph Progress Log
Started: 2026-02-12

## Codebase Patterns
- Use `LogRedactor` (Core/Support/LogRedactor.swift) for all logging of sensitive values — tokens, URLs with params, data payloads
- Apple `os.log` Logger DEBUG/INFO are NOT privacy-redacted by default; treat them as public
- Prefer logging metadata (lengths, counts, prefixes) over actual values

---

## [2026-02-12 15:30] - M9-001
- Audited all 117 Swift files for sensitive logging; found 3 issues in DiscourseAuthManager.swift
- Removed full public key PEM logging (DEBUG level)
- Removed decrypted auth payload logging (DEBUG level, contained User API Key)
- Replaced full callback URL logging with redacted version via LogRedactor
- Replaced query item value logging with name-only listing
- Created LogRedactor utility (Core/Support/LogRedactor.swift) with redactSecret, redactURL, redactData
- Updated Docs/THREAT_MODEL.md with M9 Security Hardening section documenting audit results, remediation, and assumptions
- Files changed: DiscourseAuthManager.swift, LogRedactor.swift (new), THREAT_MODEL.md, prd.json, progress.txt
- **Learnings for future iterations:**
  - os.log DEBUG logs persist in unified log and are readable via Console.app even in release builds
  - Always redact callback URLs — they may carry encrypted payloads or tokens as query/fragment params
  - Context7 did not have specific os.log privacy docs; Apple's own OSLogPrivacy API docs are the authoritative source
---
