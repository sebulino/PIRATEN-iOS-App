# Ralph Progress Log
Started: Mo.  2 Feb. 2026 12:30:35 CET
---

## Codebase Patterns

### Reply Composer UI Pattern (M4-001)
- Use `@FocusState` for managing text field focus programmatically
- Place composer at bottom of VStack with main content using `.frame(maxWidth: .infinity, maxHeight: .infinity)`
- Use `ReplyComposerState` enum to track idle/sending/sent/failed states
- Disable controls during sending state, show progress indicator on send button
- Use `TextField` with `axis: .vertical` and `lineLimit(1...6)` for expandable text input
- Keep error/success banners above the input area with dismiss actions

## [2026-02-02 16:15] - M4-001
- **What was implemented:**
  - Reply composer UI for PM thread detail view
  - Reply button in toolbar and inline "Antworten..." prompt at list bottom
  - ReplyComposerView with plain text input, send/cancel buttons
  - Proper handling of unauthenticated state (hides composer)
  - Send button disabled during sending, shows ProgressView
  - Success/error banners with dismiss functionality
  - FocusState integration for keyboard handling
- **Files changed:**
  - PIRATEN/App/ViewModels/MessageThreadDetailViewModel.swift - Added ReplyComposerState enum and composer-related properties/methods
  - PIRATEN/App/Views/Main/MessageThreadDetailView.swift - Added ReplyComposerView and integrated with thread detail
- **Context7 Docs Summary:**
  - TextField with `axis: .vertical` enables multiline expandable input
  - `@FocusState` binding with `.focused()` modifier for programmatic focus control
  - `onSubmit` modifier triggers action on Return key press
  - `disabled()` modifier prevents input during sending state
  - Text binding should be directly bound to ViewModel's `@Published` property
  - lineLimit with range (1...6) allows dynamic height growth
- **Learnings for future iterations:**
  - SwiftUI's TextField now supports multiline via `axis: .vertical` - no need for TextEditor
  - FocusState binding passed to child views works for programmatic focus
  - Composer state machine (idle/sending/sent/failed) provides clean UI feedback
  - Keep send as stub for M4-001, actual API call deferred to M4-002
---
